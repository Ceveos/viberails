{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(cat | jq -r '.tool_input.file_path // empty') && [ -n \"$FILE\" ] && node packages/cli/dist/index.js check --files \"$FILE\" --format json; exit 0"
          }
        ]
      }
    ]
  }
}
