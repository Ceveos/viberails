pre-commit:
  parallel: true
  commands:
    format:
      glob: "{packages,tests}/**/*.{ts,tsx,js,json}"
      run: npx biome format --write {staged_files}
      stage_fixed: true
    lint:
      glob: "{packages,tests}/**/*.{ts,tsx,js}"
      run: npx biome lint {staged_files}
