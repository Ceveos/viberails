import { describe, it, expect } from "vitest";

describe("hooks", () => {
  it("initializes auth state", () => {
    expect(true).toBe(true);
  });

  it("toggles theme", () => {
    expect(true).toBe(true);
  });
});
